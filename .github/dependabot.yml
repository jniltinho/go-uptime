# Part of go-uptime, derived from Gatus by TwiN (Apache-2.0); files that existed in Gatus were modified. See NOTICE.
version: 2
updates:
  # Só GitHub Actions: atualizações de gomod conflitariam com a sincronização com o upstream
  - package-ecosystem: "github-actions"
    directory: "/"
    labels: ["dependencies"]
    schedule:
      interval: "daily"
